{
  "name": "back",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "prisma generate --no-engine",
    "postinstall": "prisma generate --no-engine"
  },
  "dependencies": {
    "@prisma/client": "^6.2.1",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "prisma": "^6.2.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
